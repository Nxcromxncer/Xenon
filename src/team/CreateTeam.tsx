const CreateTeam = () => {
  return (
    <div className="team-container">
      <h2>Create a Team</h2>
      <p>Create your own team and invite others to join.</p>
      <input type="text" placeholder="Team name" />
      <button>Create Team</button>
    </div>
  );
};

export default CreateTeam;
